node_modules/
# Rendered output. Regenerated by `npm run render` / `npm run still:og`, and a
# 2 MB mp4 per render would sit in history forever. Ship the finished asset to
# the site's public/ (or a CDN) instead of versioning the renders.
out/
